[package]
name = "srelog"
version = "0.2.0"
edition = "2021"
license = "MIT"
description = "Лог дежурств: создание записи, пересборка индекса и дайджеста бэклога"

[dependencies]
chrono = { version = "0.4", default-features = false, features = ["clock", "std"] }

[profile.release]
strip = true
